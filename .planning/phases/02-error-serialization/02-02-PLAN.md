---
phase: 02-error-serialization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Responses/Serialization/ResultJsonConverter.cs
  - Responses/Serialization/ErrorJsonConverter.cs
  - Responses/Serialization/ResultJsonContext.cs
  - Responses/Responses.csproj
  - test/Responses.Tests/SerializationTests.cs
autonomous: true
domain: serialization

# Goal-backward verification
must_haves:
  truths:
    - "JsonSerializer.Serialize(result) produz JSON com IsSuccess, Value/Error, Type, Metadata"
    - "JsonSerializer.Deserialize<Result<T>>(json) preserva valor/erro (round-trip)"
    - "Error com Metadata é serializada e desserializada corretamente"
    - "ErrorCollection com múltiplos erros é serializada como array JSON"
    - "ResultJsonContext source generator é usado (sem reflection)"
  artifacts:
    - path: "Responses/Serialization/ResultJsonConverter.cs"
      provides: "STJ converter para Result, Result<T>, Result<TValue,TError>"
      min_lines: 60
    - path: "Responses/Serialization/ErrorJsonConverter.cs"
      provides: "STJ converter para Error e IError"
      min_lines: 40
    - path: "Responses/Serialization/ResultJsonContext.cs"
      provides: "JsonSerializerContext source generator"
      contains: "[JsonSerializable(typeof(Result))]"
    - path: "Responses/Responses.csproj"
      provides: "STJ package reference"
      contains: "System.Text.Json"
    - path: "test/Responses.Tests/SerializationTests.cs"
      provides: "Testes de round-trip para todos os tipos"
      min_lines: 80
  key_links:
    - from: "Responses/Serialization/ResultJsonConverter.cs"
      to: "Responses/Result.cs"
      via: "converter lê/escreve propriedades de Result"
      pattern: "Result.*Write|Result.*Read"
    - from: "Responses/Serialization/ResultJsonContext.cs"
      to: "Responses/Serialization/ResultJsonConverter.cs"
      via: "context usa converters"
      pattern: "JsonSerializerContext|JsonSerializable"
---

<objective>
Implementar serialização JSON com System.Text.Json: converters para Result/Error e source generator (zero reflection).

Purpose: Substituir Newtonsoft.Json completamente com STJ source-gen para zero reflection, AOT compatible, e 3-5x mais rápido. Round-trip fidelity é essencial para APIs.
Output: ResultJsonConverter, ErrorJsonConverter, ResultJsonContext, testes de serialização passing.
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-error-serialization/02-01-SUMMARY.md

Modelo de Error já redesignado (Phase 2 Plan 01):
@Responses/Error.cs
@Responses/IError.cs
@Responses/ErrorCollection.cs
@Responses/ErrorType.cs

Código Result existente:
@Responses/Result.cs

STJ reference:
@~/.qwen/get-shit-done/references/tdd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar System.Text.Json package e criar namespace Serialization</name>
  <files>Responses/Responses.csproj</files>
  <action>
    Em Responses/Responses.csproj, adicionar:
    ```xml
    <ItemGroup>
      <PackageReference Include="System.Text.Json" Version="10.0.0" />
    </ItemGroup>
    ```

    Nota: Para .NET 10, System.Text.Json já está incluído no framework. Mas adicionar explicitamente garante versão correta.

    Criar diretório Responses/Serialization/ para organizar os converters.
  </action>
  <verify>dotnet build passa</verify>
  <done>STJ package reference adicionada; namespace Serialization existe</done>
</task>

<task type="auto">
  <name>Task 2: Criar ErrorJsonConverter</name>
  <files>Responses/Serialization/ErrorJsonConverter.cs</files>
  <action>
    Criar Responses/Serialization/ErrorJsonConverter.cs:

    ```csharp
    public class ErrorJsonConverter : JsonConverter<IError>
    {
        public override IError? Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
        {
            // Ler JSON como object
            // Extrair: code, message, type, layer, applicationName, metadata
            // Criar Error com os valores
            // Retornar como IError
        }

        public override void Write(Utf8JsonWriter writer, IError value, JsonSerializerOptions options)
        {
            writer.WriteStartObject();
            writer.WriteString("code", value.Code);
            writer.WriteString("message", value.Message);
            writer.WriteString("type", value.Type.ToString());
            writer.WriteString("layer", value.Layer);
            writer.WriteString("applicationName", value.ApplicationName);

            if (value.Metadata.Count > 0)
            {
                writer.WriteStartObject("metadata");
                foreach (var kvp in value.Metadata)
                    writer.WriteString(kvp.Key, kvp.Value);
                writer.WriteEndObject();
            }

            writer.WriteEndObject();
        }
    }
    ```

    O Read precisa lidar com:
    - JSON com ou sem metadata
    - ErrorType como string ("Validation") → parse para enum
    - Propriedades opcionais (layer, applicationName podem estar ausentes)
  </action>
  <verify>
    JsonSerializer.Serialize<IError>(error) produz JSON com code, message, type, metadata
    JsonSerializer.Deserialize<IError>(json) recria Error com valores corretos
  </verify>
  <done>ErrorJsonConverter serializa e desserializa Error e IError corretamente</done>
</task>

<task type="auto">
  <name>Task 3: Criar ResultJsonConverter</name>
  <files>Responses/Serialization/ResultJsonConverter.cs</files>
  <action>
    Criar Responses/Serialization/ResultJsonConverter.cs com 3 converters genéricos:

    Para Result (void):
    ```csharp
    public class ResultJsonConverter : JsonConverter<Result>
    {
        public override Result Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
        {
            // Ler isSuccessful
            // Se failed: ler Error e criar Result.Fail(error)
            // Se success: criar Result.Ok()
        }

        public override void Write(Utf8JsonWriter writer, Result value, JsonSerializerOptions options)
        {
            writer.WriteStartObject();
            writer.WriteBoolean("isSuccessful", value.IsSuccess);
            if (value.IsFailed)
            {
                writer.WritePropertyName("errors");
                // Serializar Errors como array
                JsonSerializer.Serialize(writer, value.Errors, options);
            }
            writer.WriteEndObject();
        }
    }
    ```

    Para Result<T>:
    ```csharp
    public class ResultOfTJsonConverter<T> : JsonConverter<Result<T>>
    {
        // Write: { "isSuccessful": true, "value": ..., "errors": [...] }
        // Read: recriar Result.Ok(value) ou Result.Fail(errors)
    }
    ```

    Para Result<TValue, TError>:
    ```csharp
    public class ResultOfTValueTErrorJsonConverter<TValue, TError> : JsonConverter<Result<TValue, TError>>
        where TError : IError
    {
        // Similar a Result<T> mas com erro tipado
    }
    ```

    IMPORTANTE:
    - Os converters devem registrar os converters internos para Error e ErrorCollection
    - Usar `JsonSerializer.Serialize(writer, value.Errors, options)` para Errors array
    - No Read, usar `JsonSerializer.Deserialize<T>(ref reader, options)` para valor
  </action>
  <verify>
    JsonSerializer.Serialize(Result.Ok(42)) → {"isSuccessful":true,"value":42}
    JsonSerializer.Serialize(Result.Fail<int>("ERR","msg")) → {"isSuccessful":false,"errors":[...]}
    Round-trip: deserialize(serialize(result)) == result
  </verify>
  <done>ResultJsonConverter serializa/desserializa Result, Result<T>, Result<TValue,TError></done>
</task>

<task type="auto">
  <name>Task 4: Criar ResultJsonContext source generator</name>
  <files>Responses/Serialization/ResultJsonContext.cs</files>
  <action>
    Criar Responses/Serialization/ResultJsonContext.cs:

    ```csharp
    [JsonSourceGenerationOptions(
        WriteIndented = false,
        PropertyNamingPolicy = JsonKnownNamingPolicy.CamelCase,
        GenerationMode = JsonSourceGenerationMode.Default)]
    [JsonSerializable(typeof(Result))]
    [JsonSerializable(typeof(Result<>))]
    [JsonSerializable(typeof(Result<,>))]
    [JsonSerializable(typeof(Error))]
    [JsonSerializable(typeof(ErrorCollection))]
    [JsonSerializable(typeof(int))]
    [JsonSerializable(typeof(string))]
    [JsonSerializable(typeof(bool))]
    public partial class ResultJsonContext : JsonSerializerContext
    {
        public static JsonSerializerOptions DefaultOptions => new JsonSerializerOptions
        {
            TypeInfoResolver = Default,
            Converters = { new ErrorJsonConverter() },
        };
    }
    ```

    Atributos `[JsonSerializable]` para todos os tipos que serão serializados.

    Nota: `Result<>` e `Result<,>` com generic open types podem não funcionar diretamente.
    Se necessário, criar métodos factory que registram tipos concretos:
    - `[JsonSerializable(typeof(Result<int>))]`
    - `[JsonSerializable(typeof(Result<string>))]`
    - `[JsonSerializable(typeof(Result<int, Error>))]`

    Para tipos genéricos dinâmicos, o STJ fallback pode ser necessário.
  </action>
  <verify>
    dotnet build gera source code do ResultJsonContext
    JsonSerializer.Serialize(result, ResultJsonContext.Default.Result) funciona sem reflection
  </verify>
  <done>ResultJsonContext source generator compila e é usado para serialização</done>
</task>

<task type="auto">
  <name>Task 5: Escrever testes de serialização JSON (round-trip para todos os tipos)</name>
  <files>test/Responses.Tests/SerializationTests.cs</files>
  <action>
    Criar bateria de testes de serialização:

    A. Error serialização:
    - `Error_SerializeAndDeserialize_RoundTrip` — Error com code, message, type, metadata
    - `Error_Serialize_WithMetadata_IncludesMetadataInJson`
    - `Error_Deserialize_WithoutMetadata_HandlesGracefully`

    B. Result void serialização:
    - `Result_SerializeOk_RoundTrip` — Result.Ok() → JSON → Result
    - `Result_SerializeFail_RoundTrip` — Result.Fail(code, msg) → JSON → Result

    C. Result&lt;T&gt; serialização:
    - `ResultOfT_SerializeOk_RoundTrip` — Result.Ok(42) → JSON → Result<int>
    - `ResultOfT_SerializeFail_RoundTrip` — Result.Fail<int>(...) → JSON → Result<int>
    - `ResultOfT_SerializeWithStringValue` — Result.Ok("hello") → JSON → Result<string>

    D. Result&lt;TValue, TError&gt; serialização:
    - `ResultOfTValueTError_SerializeOk_RoundTrip`
    - `ResultOfTValueTError_SerializeFail_RoundTrip`
    - `ResultOfTValueTError_Serialize_WithTypedError`

    E. Multi-error serialização:
    - `Result_WithMultipleErrors_SerializesAsArray`
    - `Result_WithMultipleErrors_DeserializesCorrectly`

    F. Source generator:
    - `Serialize_UsesSourceGenerator_NoReflection` — verificar que ResultJsonContext funciona

    Usar STJ, não Newtonsoft:
    ```csharp
    var json = JsonSerializer.Serialize(result, ResultJsonContext.Default.ResultOfTInt32);
    var deserialized = JsonSerializer.Deserialize(json, ResultJsonContext.Default.ResultOfTInt32);
    ```
  </action>
  <verify>dotnet test --filter "SerializationTests" passa com todos os testes green</verify>
  <done>
    Todos os testes de serialização round-trip passing
    Error, Result, Result<T>, Result<TValue,TError> serializam e desserializam
    Multi-error funciona
    Source generator é usado
  </done>
</task>

</tasks>

<verification>
Antes de declarar plano completo:
- [ ] `dotnet build` passa sem erros ou warnings
- [ ] ErrorJsonConverter serializa/desserializa Error corretamente
- [ ] ResultJsonConverter serializa/desserializa Result, Result<T>, Result<TValue,TError>
- [ ] Round-trip: deserialize(serialize(result)) preserva valor/erro
- [ ] ResultJsonContext source generator é usado
- [ ] Multi-error serializa como array JSON
- [ ] Testes de SerializationTests passing
</verification>

<success_criteria>

- Todos os 5 tasks completed
- STJ como único serializer (Newtonsoft removido)
- ErrorJsonConverter e ResultJsonConverter funcionando
- ResultJsonContext source generator sem reflection
- JSON round-trip fidelity para todos os tipos
- Testes de serialização passing
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-error-serialization/02-02-SUMMARY.md`
</output>
