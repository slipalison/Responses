---
phase: 06-extended-status-code-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Responses/ErrorType.cs
  - Responses.Http/Http_/HttpResponseInfo.cs
  - test/Responses.Tests/StatusCodeMappingTests.cs
autonomous: true
domain: http-extensions

# Goal-backward verification
must_haves:
  truths:
    - "ErrorType enum contém TooManyRequests, BadGateway, ServiceUnavailable, GatewayTimeout"
    - "StatusCodeMapping mapeia todos os status codes 400-599 corretamente"
    - "Códigos customizados (418, 450) retornam Unknown"
    - "Códigos 4xx não mapeados retornam fallback apropriado (Validation ou melhor aproximação)"
    - "Códigos 5xx não mapeados retornam ServerError"
  artifacts:
    - path: "Responses/ErrorType.cs"
      provides: "ErrorType enum expandido com novos valores"
      min_lines: 25
    - path: "Responses.Http/Http_/HttpResponseInfo.cs"
      provides: "StatusCodeMapping.ToErrorType() completo"
      contains: "TooManyRequests|BadGateway|ServiceUnavailable"
    - path: "test/Responses.Tests/StatusCodeMappingTests.cs"
      provides: "Testes cobrindo todos os status codes HTTP 400-599"
      min_lines: 60
  key_links:
    - from: "Responses/ErrorType.cs"
      to: "Responses.Http/Http_/HttpResponseInfo.cs"
      via: "StatusCodeMapping usa ErrorType enum"
      pattern: "ErrorType\\.(TooManyRequests|BadGateway|ServiceUnavailable)"
---

<objective>
Expandir ErrorType enum e StatusCodeMapping para cobrir todos os HTTP status codes padrão (RFC 9110, RFC 6585) com fallback robusto para códigos customizados.

Purpose: Atualmente apenas 7 status codes são mapeados. HTTP pode retornar qualquer código (429, 502, 503, 504, 418, 451, etc.). Precisamos de mapeamento completo e fallback inteligente.
Output: ErrorType expandido, StatusCodeMapping completo, testes cobrindo 400-599.
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
@~/.qwen/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Código atual:
@Responses/ErrorType.cs
@Responses.Http/Http_/HttpResponseInfo.cs

Testes existentes:
@test/Responses.Tests/HttpExtensionsTests.cs (StatusCodeMappingTests - 9 testes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir ErrorType enum com novos status codes HTTP</name>
  <files>Responses/ErrorType.cs</files>
  <action>
    Adicionar ao enum ErrorType os seguintes valores (manter os existentes):

    ```csharp
    public enum ErrorType
    {
        Unknown = 0,
        Validation = 1,
        NotFound = 2,
        Conflict = 3,
        Unauthorized = 4,
        Forbidden = 5,
        ServerError = 6,
        Timeout = 7,
        Cancelled = 8,
        InternalError = 9,

        // RFC 6585 - Additional HTTP Status Codes
        TooManyRequests = 10,       // 429

        // RFC 9110 - HTTP Semantics (5xx)
        BadGateway = 11,            // 502
        ServiceUnavailable = 12,    // 503
        GatewayTimeout = 13,        // 504

        // RFC 9110 - HTTP Semantics (4xx)
        UnprocessableEntity = 14,   // 422
        Locked = 15,                // 423
        FailedDependency = 16,      // 424
        UpgradeRequired = 17,       // 426
        PreconditionRequired = 18,  // 428
        UnavailableForLegal = 19,   // 451
    }
    ```

    Manter compatibilidade — não alterar valores existentes.
    Adicionar XML doc em cada novo valor explicando o status code correspondente.
  </action>
  <verify>dotnet build passa sem erros; enum contém todos os novos valores</verify>
  <done>ErrorType enum expandido com 10 novos valores (19 total)</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever StatusCodeMapping para cobrir todos os status codes 400-599</name>
  <files>Responses.Http/Http_/HttpResponseInfo.cs</files>
  <action>
    Substituir o método StatusCodeMapping.ToErrorType() por uma switch expression completa:

    ```csharp
    public static class StatusCodeMapping
    {
        public static ErrorType ToErrorType(System.Net.HttpStatusCode statusCode) =>
            (int)statusCode switch
            {
                // 4xx Client Errors
                400 => ErrorType.Validation,
                401 => ErrorType.Unauthorized,
                402 => ErrorType.Validation,       // Payment Required (raro)
                403 => ErrorType.Forbidden,
                404 => ErrorType.NotFound,
                405 => ErrorType.Validation,       // Method Not Allowed
                406 => ErrorType.Validation,       // Not Acceptable
                407 => ErrorType.Unauthorized,     // Proxy Auth Required
                408 => ErrorType.Timeout,          // Request Timeout
                409 => ErrorType.Conflict,
                410 => ErrorType.NotFound,         // Gone
                411 => ErrorType.Validation,       // Length Required
                412 => ErrorType.Validation,       // Precondition Failed
                413 => ErrorType.Validation,       // Payload Too Large
                414 => ErrorType.Validation,       // URI Too Long
                415 => ErrorType.Validation,       // Unsupported Media Type
                416 => ErrorType.Validation,       // Range Not Satisfiable
                417 => ErrorType.Validation,       // Expectation Failed
                418 => ErrorType.Unknown,          // I'm a teapot (custom)
                421 => ErrorType.Validation,       // Misdirected Request
                422 => ErrorType.UnprocessableEntity,
                423 => ErrorType.Locked,
                424 => ErrorType.FailedDependency,
                425 => ErrorType.Validation,       // Too Early
                426 => ErrorType.UpgradeRequired,
                428 => ErrorType.PreconditionRequired,
                429 => ErrorType.TooManyRequests,
                431 => ErrorType.Validation,       // Request Header Fields Too Large
                451 => ErrorType.UnavailableForLegal,

                // 4xx Fallback — unmapped codes → Validation
                >= 400 and < 500 => ErrorType.Validation,

                // 5xx Server Errors
                500 => ErrorType.ServerError,
                501 => ErrorType.ServerError,      // Not Implemented
                502 => ErrorType.BadGateway,
                503 => ErrorType.ServiceUnavailable,
                504 => ErrorType.GatewayTimeout,
                505 => ErrorType.ServerError,      // HTTP Version Not Supported
                506 => ErrorType.ServerError,      // Variant Also Negotiates
                507 => ErrorType.ServerError,      // Insufficient Storage
                508 => ErrorType.ServerError,      // Loop Detected
                510 => ErrorType.ServerError,      // Not Extended
                511 => ErrorType.ServerError,      // Network Authentication Required

                // 5xx Fallback — unmapped codes → ServerError
                >= 500 and < 600 => ErrorType.ServerError,

                // Everything else
                _ => ErrorType.Unknown,
            };
    }
    ```

    O que EVITAR:
    - NÃO remover mapeamentos existentes (manter compatibilidade)
    - NÃO adicionar dependências externas
    - NÃO criar novos tipos — usar apenas ErrorType expandido
  </action>
  <verify>
    StatusCodeMapping.ToErrorType(429) → ErrorType.TooManyRequests
    StatusCodeMapping.ToErrorType(503) → ErrorType.ServiceUnavailable
    StatusCodeMapping.ToErrorType(504) → ErrorType.GatewayTimeout
    StatusCodeMapping.ToErrorType(418) → ErrorType.Unknown
    StatusCodeMapping.ToErrorType(999) → ErrorType.Unknown
  </verify>
  <done>StatusCodeMapping cobre todos os status codes 400-599 com fallback inteligente</done>
</task>

<task type="auto">
  <name>Task 3: Escrever testes cobrindo todos os status codes HTTP 400-599</name>
  <files>test/Responses.Tests/StatusCodeMappingTests.cs</files>
  <action>
    Criar arquivo dedicado de testes para StatusCodeMapping.

    A. Testes de status codes mapeados especificamente:
    - `ToErrorType_400_Validation`
    - `ToErrorType_401_Unauthorized`
    - `ToErrorType_403_Forbidden`
    - `ToErrorType_404_NotFound`
    - `ToErrorType_409_Conflict`
    - `ToErrorType_422_UnprocessableEntity`
    - `ToErrorType_429_TooManyRequests`
    - `ToErrorType_451_UnavailableForLegal`
    - `ToErrorType_500_ServerError`
    - `ToErrorType_502_BadGateway`
    - `ToErrorType_503_ServiceUnavailable`
    - `ToErrorType_504_GatewayTimeout`

    B. Testes de fallback para intervalos:
    - `ToErrorType_4xx_Unmapped_ReturnsValidation` (testar 420, 430, 440)
    - `ToErrorType_5xx_Unmapped_ReturnsServerError` (testar 509, 520, 599)

    C. Testes de códigos customizados:
    - `ToErrorType_418_ImATeapot_ReturnsUnknown`
    - `ToErrorType_450_BlockedByParent_ReturnsValidation` (4xx custom → Validation)
    - `ToErrorType_599_Custom_ReturnsServerError`

    D. Testes de valores fora do range:
    - `ToErrorType_200_OK_ReturnsUnknown`
    - `ToErrorType_301_Redirect_ReturnsUnknown`
    - `ToErrorType_999_Invalid_ReturnsUnknown`

    Organização:
    - Classe `StatusCodeMappingTests` com sub-classes ou regions por categoria
    - Usar [Theory] com [InlineData] para grupos de testes similares
    - Exemplo:
      ```csharp
      [Theory]
      [InlineData(400, ErrorType.Validation)]
      [InlineData(405, ErrorType.Validation)]
      [InlineData(411, ErrorType.Validation)]
      public void ToErrorType_4xx_MapsToValidation(int code, ErrorType expected)
      {
          Assert.Equal(expected, StatusCodeMapping.ToErrorType((HttpStatusCode)code));
      }
      ```
  </action>
  <verify>dotnet test --filter "StatusCodeMappingTests" passa com todos os testes green</verify>
  <done>
    Todos os testes de StatusCodeMapping passing
    Cobertura de todos os status codes 400-599 verificada
    Fallback behavior testado
    Valores fora do range testados
  </done>
</task>

</tasks>

<verification>
Antes de declarar plano completo:
- [ ] `dotnet build` passa sem erros
- [ ] ErrorType enum tem TooManyRequests, BadGateway, ServiceUnavailable, GatewayTimeout
- [ ] StatusCodeMapping.ToErrorType(429) → TooManyRequests
- [ ] StatusCodeMapping.ToErrorType(503) → ServiceUnavailable
- [ ] StatusCodeMapping.ToErrorType(504) → GatewayTimeout
- [ ] StatusCodeMapping.ToErrorType(418) → Unknown (custom code)
- [ ] StatusCodeMapping.ToErrorType(599) → ServerError (5xx fallback)
- [ ] StatusCodeMapping.ToErrorType(999) → Unknown
- [ ] Testes de StatusCodeMapping passando
</verification>

<success_criteria>

- ErrorType enum expandido com 10 novos valores
- StatusCodeMapping cobre todos os status codes 400-599
- Fallback inteligente para códigos não mapeados
- Testes cobrindo todos os status codes HTTP
- Nenhum breaking change (valores existentes preservados)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-extended-status-code-mapping/06-01-SUMMARY.md`
</output>
