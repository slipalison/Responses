---
phase: 01-core-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Responses/Responses.csproj
  - Responses/Result.cs
  - test/Responses.Tests/Responses.Tests.csproj
  - test/Responses.Tests/ResultFoundationTests.cs
autonomous: true
domain: core-library

# Goal-backward verification
must_haves:
  truths:
    - "Result, Result<T>, Result<TValue,TError> são readonly structs imutáveis"
    - "result.IsFailed existe e retorna o oposto de result.IsSuccess"
    - "result.ValueOrDefault retorna default(T) quando failed sem lançar exceção"
    - "result.Value lança InvalidOperationException quando failed"
    - "Result.Ok() e Result.Ok<T>(value) compilam e retornam IsSuccess=true"
    - "Result.Fail(code, message) compila e retorna IsSuccess=false com Error populado"
    - "[StructLayout(LayoutKind.Auto)] está aplicado em todos os structs"
  artifacts:
    - path: "Responses/Result.cs"
      provides: "Result, Result<T>, Result<TValue,TError> como readonly structs"
      contains: "public readonly struct Result"
    - path: "Responses/Responses.csproj"
      provides: "Target .NET 10, sem Newtonsoft.Json"
      contains: "net10.0"
    - path: "test/Responses.Tests/ResultFoundationTests.cs"
      provides: "Testes de imutabilidade, IsFailed, ValueOrDefault"
      min_lines: 50
  key_links:
    - from: "Responses/Result.cs"
      to: "test/Responses.Tests/ResultFoundationTests.cs"
      via: "testes validam readonly struct, IsFailed, ValueOrDefault"
      pattern: "IsFailed|ValueOrDefault|readonly struct"
    - from: "Responses/Responses.csproj"
      to: "Responses/Result.cs"
      via: "projeto compila sem Newtonsoft.Json"
      pattern: "System\\.Text\\.Json|Newtonsoft"
---

<objective>
Migrar core Result types para readonly struct imutáveis com target .NET 10, adicionar IsFailed/ValueOrDefault, e configurar projeto de testes com xUnit.

Purpose: Estabelecer a base imutável da biblioteca — sem isso, composição funcional e serialização não têm fundamento sólido.
Output: Result, Result<T>, Result<TValue,TError> como readonly structs compilando em .NET 10, testes passing.
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/1-CONTEXT.md
@.planning/REQUIREMENTS.md

Código existente para migração:
@Responses/Result.cs
@Responses/Error.cs
@Responses/Responses.csproj
@Responses/IError.cs
@Responses/ResultMessages.cs
@Responses/AssemblyContext.cs
@Responses/ResultContext.cs
@test/Responses.Tests/Responses.Tests.csproj
@test/Responses.Tests/StaticFactoriesTests.cs

Princípios de TDD:
@~/.qwen/get-shit-done/references/tdd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar csproj para .NET 10 e remover Newtonsoft.Json</name>
  <files>Responses/Responses.csproj, test/Responses.Tests/Responses.Tests.csproj</files>
  <action>
    1. Em Responses/Responses.csproj: mudar TargetFramework de netstandard2.1 para net10.0
    2. Remover PackageReference a Newtonsoft.Json (será substituído por STJ na Phase 2)
    3. Adicionar &lt;Nullable&gt;enable&lt;/Nullable&gt; e &lt;ImplicitUsings&gt;enable&lt;/ImplicitUsings&gt;
    4. Adicionar &lt;TreatWarningsAsErrors&gt;true&lt;/TreatWarningsAsErrors&gt;
    5. Adicionar &lt;GenerateDocumentationFile&gt;true&lt;/GenerateDocumentationFile&gt; (preparação para R51)
    6. Adicionar &lt;LangVersion&gt;preview&lt;/LangVersion&gt; para habilitar features C# 13
    7. Em test/Responses.Tests/Responses.Tests.csproj: mudar TargetFramework de net7.0 para net10.0
    8. Atualizar Microsoft.NET.Test.Sdk para 17.12+ e xunit para 2.9+
    9. Rodar `dotnet restore` e `dotnet build` para confirmar que compila (mesmo com warnings por enquanto)

    O que EVITAR:
    - NÃO remover referências a Newtonsoft.Json do código ainda (apenas do csproj) — vamos migrar o código na Task 2
    - NÃO tocar em Responses.Http.csproj nesta fase (Phase 3)
    - NÃO adicionar dependências novas além das que já existem
  </action>
  <verify>dotnet build Responses/Responses.csproj passa sem erros</verify>
  <done>csproj target net10.0, nullable enabled, sem Newtonsoft.Json, build passa</done>
</task>

<task type="auto">
  <name>Task 2: Converter Result, Result&lt;T&gt;, Result&lt;TValue,TError&gt; para readonly struct imutáveis</name>
  <files>Responses/Result.cs</files>
  <action>
    Reescrever os 3 structs como readonly structs imutáveis. Manter compatibilidade conceitual com factories existentes.

    Para CADA struct (Result, Result&lt;T&gt;, Result&lt;TValue,TError&gt;):

    1. Declarar como `public readonly struct Result` (adicionar readonly antes de struct)
    2. Adicionar `[StructLayout(LayoutKind.Auto)]` em cada struct (R9)
    3. Tornar TODAS as propriedades readonly (remover private set, usar init-only ou constructor assignment)
    4. Remover [JsonProperty] attributes (Newtonsoft será removido)
    5. Adicionar propriedade `IsFailed` → `public readonly bool IsFailed => !IsSuccess;` (R4)
    6. Adicionar `ValueOrDefault` property em Result&lt;T&gt; → `public readonly T ValueOrDefault => IsSuccess ? _value : default!;` (R5)
    7. Value deve continuar lançando InvalidOperationException quando failed (R2)
    8. Error deve continuar lançando quando IsSuccess (manter comportamento atual)
    9. Manter constructors internal com parâmetros (isSuccess, error, value)
    10. Manter todos os factory methods static existentes (Ok, Fail com todas as overloads)

    Para Result&lt;TValue, TError&gt;:
    - Manter constraint `where TError : IError`
    - Mesma abordagem: readonly, IsFailed, ValueOrDefault

    IMPORTANTE:
    - Usar `readonly` em TODOS os members e properties
    - Fields privados devem ser `private readonly`
    - Constructors devem atribuir todos os fields diretamente
    - NÃO implementar Map/Bind/Tap/Ensure ainda (próximo plano)
    - NÃO mudar a assinatura dos factory methods existentes (retrocompatibilidade R7, R54)
    - Remover referências a ResultMessages para o Error getter (lançar exceção direta sem mensagem customizada, ou manter a mensagem existente)
  </action>
  <verify>
    dotnet build Responses/Responses.csproj passa
    StaticFactoriesTests ainda passam (retrocompatibilidade)
  </verify>
  <done>
    3 structs são readonly structs com LayoutKind.Auto
    IsFailed property existe em todos
    ValueOrDefault existe em Result&lt;T&gt; e Result&lt;TValue,TError&gt;
    Factory methods Ok/Fail existentes compilam e funcionam
    Build passa sem erros
  </done>
</task>

<task type="auto">
  <name>Task 3: Escrever testes de fundação (IsFailed, ValueOrDefault, imutabilidade, factories)</name>
  <files>test/Responses.Tests/ResultFoundationTests.cs</files>
  <action>
    Criar bateria de testes para validar a nova fundação imutável.

    Testes obrigatórios (cada um como [Fact] separado):

    A. IsSuccess/IsFailed:
    - `IsSuccess_ReturnsTrue_ForOkResult`
    - `IsFailed_ReturnsTrue_ForFailResult`
    - `IsSuccess_IsOppositeOfIsFailed` (propriedade invariante)
    - Testar para Result, Result&lt;int&gt;, Result&lt;int, TestError&gt;

    B. Value/ValueOrDefault:
    - `Value_ThrowsInvalidOperationException_WhenFailed`
    - `ValueOrDefault_ReturnsValue_WhenSuccess`
    - `ValueOrDefault_ReturnsDefault_WhenFailed`
    - Testar para Result&lt;int&gt; e Result&lt;int, TestError&gt;

    C. Error accessor:
    - `Error_ThrowsInvalidOperationException_WhenSuccess`
    - `Error_ReturnsError_WhenFailed`

    D. Imutabilidade (compile-time verification):
    - Testar que não é possível modificar properties (isso é verificado pelo compilador com readonly struct, mas adicionar um comentário documentando)

    E. Factories retrocompatíveis:
    - `Ok_CreatesSuccessResult`
    - `OkWithValue_CreatesSuccessWithValue`
    - `FailWithString_CreatesFailedResultWithError`
    - `FailWithError_CreatesFailedResultWithError`
    - `OkIf_True_ReturnsOk` / `OkIf_False_ReturnsFail` (se OkIf já existir, senão pular)
    - `FailIf_True_ReturnsFail` / `FailIf_False_ReturnsOk` (se FailIf já existir, senão pular)

    Convenções de teste:
    - Usar xUnit [Fact] para cada caso
    - Usar Assert.Throws&lt;InvalidOperationException&gt;() para exceptions
    - Usar Shouldly-style assertions se disponível, senão Assert.True/False/Equal
    - Organizar em classes: `ResultTests`, `ResultOfTTests`, `ResultOfTValueTErrorTests`
  </action>
  <verify>dotnet test --filter "ResultFoundationTests" passa com todos os testes green</verify>
  <done>
    Todos os testes de IsSuccess/IsFailed, Value/ValueOrDefault, Error, factories passing
    Cobertura mínima dos 3 structs verificada
  </done>
</task>

</tasks>

<verification>
Antes de declarar plano completo:
- [ ] `dotnet build Responses/Responses.csproj` passa sem erros
- [ ] `dotnet test` passa (todos os testes foundation + factories existentes)
- [ ] Result, Result&lt;T&gt;, Result&lt;TValue,TError&gt; são `readonly struct` (verificar no código)
- [ ] `[StructLayout(LayoutKind.Auto)]` presente em todos os 3 structs
- [ ] `IsFailed` property existe e retorna `!IsSuccess`
- [ ] `ValueOrDefault` existe em Result&lt;T&gt; e retorna default quando failed
- [ ] `Value` lança InvalidOperationException quando failed
- [ ] Factory methods existentes (Ok, Fail) compilam e funcionam (retrocompatibilidade)
</verification>

<success_criteria>

- Todos os 3 tasks completed
- Build passa sem erros
- Testes de foundation e factories existentes passando
- readonly struct com LayoutKind.Auto aplicado
- IsFailed e ValueOrDefault implementados
- Nenhum breaking change na API surface de Ok/Fail
- Sem warnings no build (TreatWarningsAsErrors=true)
  </success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-core-foundation/01-01-SUMMARY.md`
</output>
